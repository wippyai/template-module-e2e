# Example Module

This is the registry-facing README for the published Wippy module. It is referenced by the
`ns.definition` entry in `src/_index.yaml` (`readme: file://README.md`) and rendered on the
module page in the Wippy registry.

Replace this text with documentation for your module: what it provides, the entries it exposes,
and how to depend on it from another Wippy application.
